# Define large variables as global variables to avoid memory issues caused by passing them as arguments
# `global` can only be used within the same file and cannot be used across files


usa_map_gdf = None

station_gdf = None

grid_gdf = None

W = None